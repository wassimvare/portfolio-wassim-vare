  function goal(txt,player){$('#scoreA').textContent=game.scoreA;$('#scoreB').textContent=game.scoreB;$('#countdown').textContent=txt;$('#countdown').classList.remove('hidden');spawn(game.ball.x,game.ball.y,player?'#54f4ff':'#ff5f91',70);sfx(player?520:90,.12);game.ball.x=W/2;game.ball.y=H/2;game.ball.vx=0;game.ball.vy=0;game.a.x=240;game.a.y=H/2;game.b.x=W-240;game.b.y=H/2;setTimeout(()=>$('#countdown').classList.add('hidden'),650)}
  function spawn(x,y,color,n){for(let i=0;i<n;i++)game.particles.push({x,y,vx:(Math.random()-.5)*9,vy:(Math.random()-.5)*9,a:1,color,size:2+Math.random()*5})}
  function draw(){
    ctx.clearRect(0,0,W,H);let g=ctx.createRadialGradient(W/2,H/2,10,W/2,H/2,700);g.addColorStop(0,'#13183a');g.addColorStop(.55,'#090d1c');g.addColorStop(1,'#04060b');ctx.fillStyle=g;ctx.fillRect(0,0,W,H);
    ctx.strokeStyle='rgba(84,244,255,.08)';ctx.lineWidth=1;for(let x=40;x<W;x+=60){ctx.beginPath();ctx.moveTo(x,40);ctx.lineTo(x,H-40);ctx.stroke()}for(let y=40;y<H;y+=60){ctx.beginPath();ctx.moveTo(40,y);ctx.lineTo(W-40,y);ctx.stroke()}
    ctx.strokeStyle='rgba(255,255,255,.12)';ctx.lineWidth=3;roundRect(ctx,34,34,W-68,H-68,28);ctx.stroke();ctx.beginPath();ctx.moveTo(W/2,34);ctx.lineTo(W/2,H-34);ctx.stroke();ctx.beginPath();ctx.arc(W/2,H/2,118,0,Math.PI*2);ctx.stroke();
    drawGoal(28,H/2-115,18,230,'#54f4ff');drawGoal(W-46,H/2-115,18,230,'#ff5f91');
    drawPlayer(game.a);drawPlayer(game.b);drawBall(game.ball);game.particles.forEach(p=>{p.x+=p.vx;p.y+=p.vy;p.vx*=.97;p.vy*=.97;p.a-=.025;ctx.globalAlpha=Math.max(0,p.a);ctx.fillStyle=p.color;ctx.beginPath();ctx.arc(p.x,p.y,p.size,0,Math.PI*2);ctx.fill()});ctx.globalAlpha=1;game.particles=game.particles.filter(p=>p.a>0);
  }
  function roundRect(c,x,y,w,h,r){c.beginPath();c.roundRect(x,y,w,h,r)}
  function drawGoal(x,y,w,h,color){ctx.save();ctx.shadowColor=color;ctx.shadowBlur=22;ctx.fillStyle=color;ctx.globalAlpha=.7;ctx.fillRect(x,y,w,h);ctx.restore()}
  function drawPlayer(p){p.trail.forEach((t,i)=>{ctx.globalAlpha=(i/p.trail.length)*.15;ctx.fillStyle=p.color;ctx.beginPath();ctx.arc(t.x,t.y,p.r*(.45+i/p.trail.length*.35),0,Math.PI*2);ctx.fill()});ctx.globalAlpha=1;ctx.save();ctx.translate(p.x,p.y);let ang=Math.atan2(p.vy,p.vx);ctx.rotate(ang);ctx.shadowColor=p.color;ctx.shadowBlur=24;ctx.fillStyle=p.color;ctx.beginPath();ctx.moveTo(38,0);ctx.lineTo(-20,-25);ctx.lineTo(-8,0);ctx.lineTo(-20,25);ctx.closePath();ctx.fill();ctx.shadowBlur=0;ctx.fillStyle='#061019';ctx.beginPath();ctx.arc(4,0,10,0,Math.PI*2);ctx.fill();ctx.restore()}
  function drawBall(b){ctx.save();ctx.shadowColor='#b78cff';ctx.shadowBlur=30;let g=ctx.createRadialGradient(b.x-9,b.y-10,3,b.x,b.y,b.r);g.addColorStop(0,'#ffffff');g.addColorStop(.17,'#7cf7ff');g.addColorStop(.55,'#805cff');g.addColorStop(1,'#18204d');ctx.fillStyle=g;ctx.beginPath();ctx.arc(b.x,b.y,b.r,0,Math.PI*2);ctx.fill();ctx.restore()}
  function loop(now){if(!game||!game.running)return;const dt=Math.min(.035,(now-last)/1000||.016);last=now;physics(dt);draw();if(game&&game.running)raf=requestAnimationFrame(loop)}
  function endMatch(){if(!game||!game.running)return;game.running=false;cancelAnimationFrame(raf);const won=game.scoreA>game.scoreB;state.matches++;if(state.placements<3)state.placements++;if(won)state.wins++;state.goals+=game.playerGoals;state.xp+=won?75:42;state.credits+=won?620:340;state.shards+=won?2:1;const drop=rollDrop(won);state.inventory.push(drop);state.history.push(`${won?'Victoire':'Défaite'} ${game.scoreA}-${game.scoreB} • drop ${itemById(drop.id).name}`);save();setTimeout(()=>showReward(won,drop),400)}
  function rollDrop(won){let r=Math.random(),rar;if(r<.01+(won?.012:0))rar='Mythique';else if(r<.055)rar='Légendaire';else if(r<.19)rar='Épique';else if(r<.55)rar='Rare';else rar='Commun';const pool=catalog.filter(i=>i.rarity===rar),i=pool[Math.floor(Math.random()*pool.length)];return{id:i.id,serial:i.max?Math.floor(Math.random()*i.max)+1:Math.floor(Math.random()*99999)+1,wins:0,owners:1}}
  function showReward(won,o){const i=itemById(o.id);nav('play');showModal(`<p class="eyebrow">MATCH COMPLETE</p><h2>${won?'VICTOIRE':'MATCH TERMINÉ'}</h2><p>${won?'+620':'+340'} NC • ${won?'+75':'+42'} XP • +${won?2:1} shard${won?'s':''}</p><div class="reward-art" style="${styleVars(i)}"></div><p style="text-align:center"><span class="rarity ${rarityClass(i.rarity)}">${i.rarity.toUpperCase()}</span></p><h3 style="text-align:center">${i.name}</h3><p style="text-align:center">Série #${o.serial}${i.max?` / ${i.max}`:''}</p><button class="primary-btn" style="width:100%" onclick="document.querySelector('#modalClose').click()">AJOUTÉ AU VAULT</button>`)}
  // touch controls — robust on Safari/iPhone
  let touchVec={x:0,y:0},joyId=null;const joy=$('#joystick'),stick=$('#stick');
  const resetJoy=()=>{joyId=null;touchVec={x:0,y:0};stick.style.transform='translate(0,0)'};
  function updateJoy(e){const r=joy.getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2,dx=e.clientX-cx,dy=e.clientY-cy,d=Math.hypot(dx,dy),limit=Math.max(34,(r.width-50)/2-5),m=Math.min(limit,d),nx=d?dx/d:0,ny=d?dy/d:0;touchVec={x:nx*(m/limit),y:ny*(m/limit)};stick.style.transform=`translate(${nx*m}px,${ny*m}px)`}
  joy.addEventListener('pointerdown',e=>{e.preventDefault();joyId=e.pointerId;try{joy.setPointerCapture(joyId)}catch{}updateJoy(e)});
  joy.addEventListener('pointermove',e=>{if(e.pointerId===joyId){e.preventDefault();updateJoy(e)}});
